#!/system/bin/sh

for cache in /data/*/*/cache; do
[ -d /${cache} ] && {
     echo "cache found in ${cache}" && rm -rf ${cache}
}
[ -d /${cache} ] || {
     echo "cache removed"
}
done

# linex Project 25/10/25