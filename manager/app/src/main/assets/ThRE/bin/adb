#!/system/bin/sh
# adb: wrapper to run adb from terminal
# osm0sis @ xda-developers
# ThRE Team

dir="$(cd "$(dirname "$0")"; pwd)";
pub="$FILES/tmp";
mkdir -p $pub
export HOME=$FILES/tmp;
export TMPDIR=$FILES/tmp;
$dir/adb.bin "$@";

