PORT="5555"
echo "starting adb wifi services...."
setprop service.adb.tcp.port $PORT
stop adbd && start adbd
echo
adb devices
adb kill-server
#ip -o a
echo 'to connect to this devices:'
echo "adb connect <ip>:$PORT"
